<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorLighterBlack</item>
        <item name="colorPrimaryDark">@color/colorMainBlack</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Widget.BottomNavigationViewCustom" parent="@style/Widget.Design.BottomNavigationView">
        <item name="fontFamily">@font/quarkbold</item>
    </style>

</resources>
